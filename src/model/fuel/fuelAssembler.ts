import {readFileSync} from "fs";
import {FileStorage} from "../storage/file/FileStorage";

export class FuelAssembler {
    public static convertInstant() {
        const json = FileStorage.store.get("instant.json");

    }
}